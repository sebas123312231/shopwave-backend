spring.config.activate.on-profile=demo
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:shopwave_demo}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
shopwave.seed.enabled=true
shopwave.demo-admin.enabled=${SHOPWAVE_DEMO_ADMIN_ENABLED:false}
